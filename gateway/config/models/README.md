# Gateway Models

This directory is reserved for future Express Gateway model configuration if the gateway is swapped from the current lightweight proxy runtime to the full CLI-managed gateway setup.
