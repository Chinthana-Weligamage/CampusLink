# Project Plan Mirror

The primary implementation plan lives at `documentation/PLAN.md`.

This file exists so the generated project structure matches the assignment-facing `docs/` layout described in the plan.
