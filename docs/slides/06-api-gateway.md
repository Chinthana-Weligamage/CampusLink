# API Gateway

- Purpose: single public entry point and docs proxy surface
- Route prefixes: `/user`, `/transport`, `/assignment`, `/notification`
- Responsibilities: proxying, request logging, CORS, stable public URLs
- Capture later: gateway route index and proxied Swagger pages
